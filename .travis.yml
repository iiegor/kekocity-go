sudo: true
language: go

notifications:
  email:
    - dextrackmedia@gmail.com

before_install:
  - git submodule update --init --recursive

install:
  - export PATH=$HOME/gopath/bin:$PATH
  - go get -v -d ./...

script: make

go:
  - 1.4.2
